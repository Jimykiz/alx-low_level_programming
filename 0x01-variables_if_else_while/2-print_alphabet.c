#include <stdio.h>
/**
 * main - This the main function
 *
 * Return: it will return (0) if successful
 */
int main(void)
{
char ch = 'a';
for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
putchar('\n');
return (0);
}
