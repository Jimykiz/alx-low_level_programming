#include "main.h"
/**
 * print_alphabet_x10 - prints the alphabet in lowercase
 * followed by new line.
 *
 * Return: Always (0)
 */
void print_alphabet_x10(void)
{
	char c, i;

for (i = '0'; i <= '9'; i++)
{
for (c = 'a'; j <= 'z'; j++)
{
_putchar(c);
}
_putchar('\n');
}
}
